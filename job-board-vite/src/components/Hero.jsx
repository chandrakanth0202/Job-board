export default function Hero() {
  return (
    <section className="hero">
      <p className="hero-eyebrow">Hitech City · Hyderabad</p>
      <h1 className="hero-title">Jobs that run after dark.</h1>
      <p className="hero-subtitle">
        Onsite, night-shift roles from teams that keep the lights on after
        everyone else has gone home.
      </p>
    </section>
  );
}
